name: "CodeQL Configuration"

# Exclude build artifacts and temporary files
paths-ignore:
  - 'build/**'
  - '**/build/**'
  - '**/.meson-private/**'

# Exclude specific noisy queries
query-filters:
  - exclude:
      id: cpp/short-global-name
