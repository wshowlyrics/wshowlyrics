name: Upload to COPR

on:
  workflow_run:
    workflows: ["Security Scan"]
    types:
      - completed

permissions:
  contents: read

concurrency:
  group: copr-upload
  cancel-in-progress: false

jobs:
  upload-copr:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Security check - Reject forks
        run: |
          if [ "${{ github.event.workflow_run.head_repository.full_name }}" != "${{ github.repository }}" ]; then
            echo "::error::Security: Refusing to run workflow from fork"
            echo "Fork repository: ${{ github.event.workflow_run.head_repository.full_name }}"
            echo "Expected repository: ${{ github.repository }}"
            exit 1
          fi

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          fetch-depth: 0

      - name: Check for release tag
        id: check_tag
        run: |
          TAG=$(git tag --points-at HEAD | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' | head -1)

          if [ -z "$TAG" ]; then
            echo "has_tag=false" >> $GITHUB_OUTPUT
            echo "No release tag found, skipping COPR upload"
          else
            echo "has_tag=true" >> $GITHUB_OUTPUT
            VERSION="${TAG#v}"
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "tag=$TAG" >> $GITHUB_OUTPUT
            echo "Release tag found: $TAG (version: $VERSION)"
          fi

      - name: Install COPR CLI
        if: steps.check_tag.outputs.has_tag == 'true'
        run: |
          pip install copr-cli

      - name: Setup COPR config
        if: steps.check_tag.outputs.has_tag == 'true'
        run: |
          mkdir -p ~/.config
          echo "${{ secrets.COPR_CONFIG }}" > ~/.config/copr
          chmod 600 ~/.config/copr

      - name: Create .spec file
        if: steps.check_tag.outputs.has_tag == 'true'
        run: |
          VERSION="${{ steps.check_tag.outputs.version }}"

          cat > wshowlyrics.spec << 'SPECEOF'
          Name:           wshowlyrics
          Version:        VERSION_PLACEHOLDER
          Release:        1%{?dist}
          Summary:        Wayland-native lyrics display with online fallback

          License:        GPL-3.0-or-later
          URL:            https://github.com/unstable-code/lyrics
          Source0:        https://github.com/unstable-code/lyrics/archive/refs/tags/vVERSION_PLACEHOLDER.tar.gz

          BuildRequires:  meson >= 0.48.0
          BuildRequires:  ninja-build
          BuildRequires:  gcc
          BuildRequires:  wayland-devel
          BuildRequires:  wayland-protocols-devel
          BuildRequires:  cairo-devel
          BuildRequires:  pango-devel
          BuildRequires:  libcurl-devel
          BuildRequires:  fontconfig-devel
          BuildRequires:  libappindicator-gtk3-devel
          BuildRequires:  gdk-pixbuf2-devel
          BuildRequires:  json-c-devel
          BuildRequires:  openssl-devel

          Requires:       wayland
          Requires:       cairo
          Requires:       pango
          Requires:       libcurl
          Requires:       fontconfig
          Requires:       libappindicator-gtk3
          Requires:       gdk-pixbuf2
          Requires:       playerctl
          Requires:       json-c
          Requires:       openssl-libs

          Recommends:     sway

          %description
          wshowlyrics is a lightweight lyrics overlay for Wayland compositors
          that displays synchronized lyrics for currently playing music.

          Features:
          - MPRIS2 integration for music player detection
          - Local lyrics file support (.lrc, .lrcx, .srt)
          - Online lyrics fetching from lrclib.net
          - Karaoke-style word-level timing
          - AI-powered translation support
          - System tray integration with album art

          %prep
          %autosetup -n lyrics-VERSION_PLACEHOLDER

          %build
          %meson
          %meson_build

          %install
          %meson_install
          # Rename binary from lyrics to wshowlyrics
          mv %{buildroot}%{_bindir}/lyrics %{buildroot}%{_bindir}/wshowlyrics

          %post
          %systemd_user_post wshowlyrics.service

          %preun
          %systemd_user_preun wshowlyrics.service

          %files
          %license LICENSE
          %doc README.md
          %{_bindir}/wshowlyrics
          %config(noreplace) %{_sysconfdir}/wshowlyrics/settings.ini
          %{_userunitdir}/wshowlyrics.service

          %changelog
          * $(date +"%a %b %d %Y") unstable-code <assa0620@gmail.com> - VERSION_PLACEHOLDER-1
          - Release VERSION_PLACEHOLDER
          SPECEOF

          sed -i "s/VERSION_PLACEHOLDER/${VERSION}/g" wshowlyrics.spec

      - name: Build SRPM
        if: steps.check_tag.outputs.has_tag == 'true'
        run: |
          VERSION="${{ steps.check_tag.outputs.version }}"

          # Download source tarball from GitHub
          wget "https://github.com/unstable-code/lyrics/archive/refs/tags/v${VERSION}.tar.gz"

          docker run --rm -v "$PWD:/workspace" -w /workspace fedora:latest bash -c "
            dnf install -y rpm-build rpmdevtools wget
            mkdir -p ~/rpmbuild/{BUILD,RPMS,SOURCES,SPECS,SRPMS}
            cp v${VERSION}.tar.gz ~/rpmbuild/SOURCES/
            cp wshowlyrics.spec ~/rpmbuild/SPECS/
            rpmbuild -bs ~/rpmbuild/SPECS/wshowlyrics.spec
            cp ~/rpmbuild/SRPMS/wshowlyrics-*.src.rpm /workspace/
          "

      - name: Upload to COPR
        if: steps.check_tag.outputs.has_tag == 'true'
        run: |
          VERSION="${{ steps.check_tag.outputs.version }}"
          SRPM=$(find . -maxdepth 1 -name "wshowlyrics-*.src.rpm")

          copr-cli build unstable-code/wshowlyrics "${SRPM}"

      - name: Wait for COPR build
        if: steps.check_tag.outputs.has_tag == 'true'
        run: |
          echo "COPR build submitted. Check status at:"
          echo "https://copr.fedorainfracloud.org/coprs/unstable-code/wshowlyrics/builds/"
